# AGENTS.md

## Scope

This file applies only to files under the top-level `analyzer/` directory.

Analyzer source code lives under `src/analyzer/`. Source-code instructions for that layer live in `src/analyzer/AGENTS.md`.

## Rules

- Do not add analyzer source code under this top-level directory.
- For analyzer implementation work, read root `AGENTS.md`, then `docs/codex/index.md`, then `docs/analyzer.md` and `docs/cost.md`.
- Keep this file as a short scope pointer so AGENTS.md scoping stays accurate.
